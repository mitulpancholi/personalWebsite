export type BlogDetailsProps = {
  title: string,
};

const BlogDetails = ({ title }: BlogDetailsProps) => {

  return (
    <>
      <h1>Welcome to Blog</h1>
    </>
  );
};

export default BlogDetails;
